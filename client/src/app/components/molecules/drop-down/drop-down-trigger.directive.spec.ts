import { DropDownTriggerDirective } from './drop-down-trigger.directive';

describe('DropDownDirective', () => {
    it('should create an instance', () => {
        const directive = new DropDownTriggerDirective();
        expect(directive).toBeTruthy();
    });
});
